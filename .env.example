# Example environment variables for ThermalSR Pro frontend
# Copy this file to `.env` for local development or use the values below as templates
# Set production values in Vercel Project Settings (Environment Variables) instead of committing secrets.
# Vite only exposes variables that begin with VITE_

VITE_API_URL=https://api.example.com
VITE_PUBLIC_URL=https://your-domain.com
VITE_GA_MEASUREMENT_ID=
VITE_SENTRY_DSN=
VITE_FEATURE_FLAGS=
